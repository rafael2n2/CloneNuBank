import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    container: {
        flex: 1,
    },

    CardMeusCartoesView:{
        padding: 24,
        marginTop: 24,
    },

    BorderSeparetion:{
        borderTopWidth: 1,
        marginTop: 8,
        borderColor: '#e6e6e6',
    },

});

export default styles